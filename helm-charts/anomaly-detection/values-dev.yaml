# DEV environment overlay
global:
  environment: "DEV"

config:
  cluster: "bdc-workloads-dev-nv"
  datasources:
    victoriametrics:
      url: "http://vm-cluster-vmselect.monitoring:8481/select/0/prometheus"
    loki:
      url: "http://loki-gateway.monitoring:80"
    alertmanager:
      url: "http://prometheus-alertmanager.monitoring:9093"
  ml:
    enabled: false

controller:
  replicas: 1
  image:
    tag: ""  # CI sets this

worker:
  replicas: 2
  image:
    tag: ""  # CI sets this

redis:
  persistence:
    size: 10Gi

ml:
  enabled: false
